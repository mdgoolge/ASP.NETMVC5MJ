<h4>Bootstrap和jQuery UI模式对话框的基本用法</h4>
<button class="btn btn-info" data-toggle="modal" data-target="#modalDialog1">
    显示Bootstrap模式对话框
</button>
<div id="modalDialog1" class="modal fade">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">提示</h4>
            </div>
            <div class="modal-body">
                <p>是否保存正在处理的数据？</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal">保存</button>
                <button type="button" class="btn btn-default" data-dismiss="modal">放弃</button>
            </div>
        </div>
    </div>
</div>

<button id="btn2" class="btn btn-primary">
    显示jQueryUI模式对话框
</button>
<div id="modalDialog2">
    <p>确认要删除吗？</p>
</div>
<script>
    //解决不显示jQueryUI右上角关闭按钮的冲突
    try { $.fn.button.noConflict(); } catch (e) { }

    $("#modalDialog2").dialog({
        closeText: "取消",  //鼠标悬停到右上角关闭按钮时自动显示的工具提示，默认"Cancel"
        modal: true,      //是否为模式对话框，默认false
        autoOpen: false,  //默认true
        height: "auto",  //除了"auto"以外，其他情况都应该是数值
        //minHeight:140,
        //minWidth:600,
        buttons: {
            "删除": function () { $(this).dialog("close"); },
            "取消": function () { $(this).dialog("close"); }
        }
    });
    $("#btn2").click(function () {
        $("#modalDialog2").dialog("option", { title: "警告" });
        $("#modalDialog2").dialog("open");
    });
</script>